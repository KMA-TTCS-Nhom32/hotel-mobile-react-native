import { LoginScreen } from '@/screens/auth';

/**
 * Page component that renders the authentication login screen.
 *
 * Renders the shared `LoginScreen` component and serves as the default export for the auth login route.
 *
 * @returns The JSX element for the login page.
 */
export default function LoginPage() {
  return <LoginScreen />;
}
