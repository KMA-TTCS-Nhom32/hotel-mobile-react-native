import { RegisterScreen } from '@/screens/auth';

/**
 * Page component that renders the authentication RegisterScreen.
 *
 * This default-exported React component is the page entry for /auth/register and delegates rendering to the shared RegisterScreen component.
 *
 * @returns The JSX element for the registration screen.
 */
export default function RegisterPage() {
  return <RegisterScreen />;
}
